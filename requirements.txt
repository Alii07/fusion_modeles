# Streamlit for web applications
streamlit>=1.0.0

# Scikit-learn for machine learning
scikit-learn>=1.2.0

# Pandas for data manipulation
pandas>=1.3.0

# Joblib for model serialization
joblib>=1.0.0

# Numpy for numerical operations
numpy>=1.21.0

# TensorFlow and Keras for deep learning models
tensorflow>=2.9.0
keras>=2.9.0  # Optionnel si déjà inclus dans TensorFlow, mais utile pour clarté

# Pyngrok for tunneling (useful for local testing)
pyngrok>=5.0.0

# Visualization libraries
altair>=4.1.0
matplotlib>=3.4.0
kaleido>=0.2.1  # Pour générer des images via Altair ou Plotly dans un environnement sans interface graphique

# Camelot for PDF table extraction (sans l'option [cv])
camelot-py>=0.11.0

# OpenCV version légère
opencv-python-headless>=4.5.0  # OpenCV séparément pour éviter les conflits avec pdftopng

# PyPDF for PDF manipulation
pypdf>=3.0.0

# Ghostscript for handling PDFs (required by Camelot)
ghostscript>=0.7

camelot-py[cv]

